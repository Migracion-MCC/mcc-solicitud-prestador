const FormSelectInput = () => {
  return (
    <select className={"border-2 p-1 rounded"}>
      <option value="">Seleccione una opcion</option>
    </select>
  );
};

export default FormSelectInput;
